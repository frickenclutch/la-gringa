# Cloudflare Pages only (the legacy la-gringa.pages.dev mirror).
# Everything 301s to the canonical site on the brand domain, path and query
# intact. The one exception is the service worker: it is still served from
# here so an app installed on this origin can update to the version that
# retires itself (see sw.js) instead of answering from a stale cache.
# NOTE: never stage this into dist/ — the Worker serving it would
# redirect to itself in a loop (tools/stage-assets.mjs excludes it).
/sw.js /sw.js 200
/* https://dirtygringonny.com/:splat 301
